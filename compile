#!/bin/sh
# Ruby front-end for your compiler.
# You are free to change the language used for this script,
# but do *not* change its name.

# puts "TODO: setup this script to call your WACC compiler"

#file = ARGV[0]
#if file == nil
 #puts "Please, enter the folder below: \n"
 #file = gets.chomp
#end

java -cp bin:lib/antlr-4.7-complete.jar src/WaccMain $1 < $1
exit $?



#puts file
# call main class
    #system("make")
    #system("mvn package")
    #cmd = IO.popen("java -classpath ../lib/antlr-4.7-complete.jar:. src/TestLexer");